---
import Avatar from "./Avatar";
import Logo from "./Logo.astro";
import BackButton from "./BackButton.astro";
export interface Props {
  backButton?: boolean;
}

const { backButton } = Astro.props as Props;
---

<nav class="nav border-b border-gray-800 sticky top-0 z-30 bg-gray-800">
  <div
    class="container flex flex-wrap items-center justify-between mx-auto p-4"
  >
    <!-- <div class="container mx-auto px-4 py-4 flex flex-row">
	</div> -->
    <BackButton visible={backButton} text="Volver" />
    <a href="/" class="flex items-center font-bold text-xl">
      <div class="mb-2 mr-1"><Logo /></div>
      <span class="mr-2 text-red-600">op</span>
      <span> pelis</span>
    </a>
    <div
      class="flex items-center md:order-2 space-x-3 md:space-x-0 rtl:space-x-reverse mr-1 md:mr-4"
    >
      <Avatar client:load />
    </div>
  </div>
</nav>
