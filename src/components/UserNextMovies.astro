---
import NextMovies from "./NextMovies";
import { getNextUserMovies } from "lib/turso";

const nextAnonymousMovies = await getNextUserMovies(); // default movies without user (anonymous)
---

<NextMovies initialNextMovies={nextAnonymousMovies.slice(0, 5)} client:load />
